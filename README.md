Redis Presentation and Source Code
==================================

src/ - A Spring Boot application with three examples (Auth, Feature Flags, HyperLogLog/User Counts)
slides/ - the slides

Prereqs:
    - A running Redis instance (localhost)
    - Java 1.7
    - Maven
